<?xml version="1.0" encoding="utf-8"?>
<!-- PhotoView manages its own image matrix; do not use changeImageTransform (causes over-zoom with shared elements). -->
<transitionSet xmlns:android="http://schemas.android.com/apk/res/android">
    <changeBounds />
    <changeTransform />
</transitionSet>
